# SAN 개인 통합 저장소 작업 지침

- 한국어 `~습니다`체로 완료·미완료·검증 근거를 구분합니다.
- 통합 범위는 `frontend/`, `backend/`, `ai/`뿐입니다. 조직의 `docs`, `san-engine`, `.github` 저장소를 자동 추가하지 않습니다.
- 기존 팀 원본과 작성자·커밋 이력을 보존합니다. 개인 기여는 이 통합 이후 변경과 근거로 구분합니다.
- 서비스별 패키지 관리자와 빌드 디렉터리를 유지합니다. 폴더 통합을 이유로 의존성이나 기술 스택을 갱신하지 않습니다.
- 각 서비스의 하위 지침을 함께 읽습니다. 실제 발급받지 않은 Jira 티켓 번호를 만들지 않습니다.
- 원본 `source-*` 원격에는 push하지 않습니다. 개인 `origin`으로의 공개 push·PR·배포도 실행 직전에 사용자 승인을 확인합니다.
- 원본의 서비스별 `.github/workflows`를 루트로 옮기거나 운영 배포·GitLab 동기화·외부 알림을 활성화하지 않습니다.
- `.env`, 비밀키, 운영 데이터는 커밋하지 않습니다. 외부 AI 유료 호출·실서비스 접근은 별도 승인 없이 수행하지 않습니다.
- Git 이력 보존 확인은 `python3 scripts/verify-imports.py`를 사용합니다. 이후 개선으로 달라진 서비스 트리는 초기 이관 검증과 구분합니다.
